<!--pages/partner/partner.wxml-->
<view class="container">
  <!-- 吸顶筛选区：仅保留同事搭/校友搭/Pick搭 -->
  <view class="partner-header-fixed">
    <!-- 搜索+筛选小区域暂时注释
    <view class="top-bar">
      <view class="search-box" bindtap="goToFilter">
        <image class="search-icon" src="/images/search.png" mode="aspectFit" />
        <text>搜索搭子...</text>
      </view>
      <view class="filter-btn" bindtap="goToFilter">
        <text>筛选</text>
      </view>
    </view>
    -->
    <view class="scope-tabs">
      <view 
        class="scope-tab {{activeScope === index ? 'active' : ''}}"
        wx:for="{{scopeTabs}}"
        wx:key="key"
        bindtap="selectScope"
        data-index="{{index}}"
      >
        <text class="scope-tab-text">{{item.label}}</text>
        <view class="scope-tab-underline" wx:if="{{activeScope === index}}"></view>
      </view>
    </view>
  </view>
  <view class="partner-header-placeholder"></view>

  <!-- 搭子列表：和首页一样用整页滚动，不用固定高度 scroll-view，避免底部白块遮挡 -->
  <view class="partner-list" wx:if="{{!isLoading}}">
    <block wx:for="{{partners}}" wx:key="id">
      <view class="partner-card" bindtap="goToDetail" data-id="{{item.id}}">
        <view class="partner-cover-wrap">
          <network-image
            class="partner-cover"
            customClass="partner-cover"
            url="{{item.cover}}"
            defaultSrc="/images/partner-banner.jpg"
            mode="aspectFill"
          />
          <view class="partner-status-tag {{item.status}}">
            {{item.statusLabel || (item.status === 'recruiting' ? '招募中' : (item.status === 'full' ? '已满' : '已结束'))}}
          </view>
        </view>
        <view class="partner-body">
          <view class="partner-header">
            <view class="partner-category">{{item.typeName || item.category}}</view>
          </view>
          <view class="partner-title">{{item.title}}</view>
          <view class="partner-pref" wx:if="{{item.preference}}">偏好：{{item.preference}}</view>
          <view class="partner-tags">
            <text class="tag" wx:for="{{item.tags}}" wx:for-item="tag" wx:key="*this">{{tag}}</text>
          </view>
          <view class="partner-footer">
            <view class="author-info">
              <network-image
                class="author-avatar"
                customClass="author-avatar"
                url="{{item.author.avatar}}"
                defaultSrc="/images/default-avatar.png"
                mode="aspectFill"
              />
              <text class="author-name">{{item.author.name}}</text>
              <text class="author-distance" wx:if="{{item.distance}}">| {{item.distance}}</text>
            </view>
            <view class="match-display" wx:if="{{item.match}}">
              <text class="match-num">{{item.match}}%</text>
              <text class="match-label">匹配</text>
            </view>
          </view>
          <view class="member-info">
            <text class="member-count">👥 {{item.members}}/{{item.maxMembers}}人</text>
            <text class="partner-time">{{item.createTime}}</text>
          </view>
        </view>
      </view>
    </block>

    <!-- 空状态 -->
    <view class="empty-state" wx:if="{{partners.length === 0}}">
      <image src="/images/empty-partner.png" mode="aspectFit" />
      <text>暂无搭子信息</text>
    </view>
  </view>

  <!-- 加载中 -->
  <view class="loading" wx:else>
    <text>加载中...</text>
  </view>

  <!-- 发布按钮：跳转到发布搭子二级页 -->
  <view class="publish-btn" bindtap="goToPublish">
    <text class="publish-btn-icon">+</text>
  </view>
</view>