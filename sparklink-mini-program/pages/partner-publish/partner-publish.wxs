/** 判断标签是否已选 */
function includes(arr, label) {
  if (!arr || !arr.length) {
    return false;
  }
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === label) {
      return true;
    }
  }
  return false;
}

module.exports.includes = includes;
