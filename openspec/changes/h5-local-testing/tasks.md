## 1. 环境准备

- [x] 1.1 在 `sparklink-h5/` 执行 `npm install`，确认无报错
- [x] 1.2 执行 `npm run build`，确认 `dist/` 生成成功
- [x] 1.3 复制 `.env.development` 为 `.env.development.local`（如需本地后端则设置 `VITE_API_BASE_URL=http://127.0.0.1:8080`）
- [x] 1.4 准备测试账号（用户名/密码），确认非 `test-login` 依赖

## 2. 启动与连通性

- [x] 2.1 运行 `npm run dev`，浏览器打开 `http://localhost:5173`
- [x] 2.2 打开开发者工具 Network，确认 `/api/health` 或登录请求返回 200
- [x] 2.3 若使用本地后端：先 `curl http://127.0.0.1:8080/api/health` 通过后再测 H5

## 3. P0 功能冒烟（对照 `部署/H5-UAT-CHECKLIST.md`）

- [x] 3.1 登录：账号密码登录成功，刷新页面仍保持登录态
- [ ] 3.2 401：清除 token 或用过期 token 触发跳转登录页
- [x] 3.3 首页 → AI 聊天：发送消息并收到回复
- [x] 3.4 搭子：三 Tab 列表加载；进入详情页正常
- [ ] 3.5 搭子详情 → 联系 TA：进入 IM 聊天并发送一条消息
- [ ] 3.6 发布搭子：填写标题/类型/描述，可选上传封面，提交成功
- [ ] 3.7 消息列表：IM 会话加载，Tab 未读角标有更新
- [ ] 3.8 我的 → 编辑资料：改昵称或头像并保存成功
- [x] 3.9 退出登录：会话清除，消息页提示需重新登录

## 4. 移动端与兼容性

- [ ] 4.1 Chrome DevTools 设备模拟（iPhone 375px）走一遍 3.1–3.5
- [ ] 4.2 Safari（真机或模拟器）抽查登录 + 搭子列表

## 5. 问题记录与放行

- [x] 5.1 将失败项记录到 change 备注（现象、接口、截图路径）
- [x] 5.2 修复阻塞项后重跑对应用例
- [x] 5.3 全部 P0 通过后，在部署日志注明：日期、测试人、API 环境（线上/本地）
- [x] 5.4 更新 `sparklink-h5/README.md` 本地测试小节（若实施中发现文档缺口）
